artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Parse an Apache-style access log into a JSON traffic summary report."

[metadata]
category = "data_processing_and_etl"
subcategory = "text_processing"
task_objective = "transform"
artifact_type = "generated_output_artifact"
expert_time_estimate_hours = 0.3
model_tested = "gpt_5_4"
agent_tested = "terminus_2"
difficulty_explanation = "Parse a small Apache-style access log and produce a deterministic JSON summary."
solution_explanation = "Read each non-empty log entry, count requests and distinct client IP addresses, determine the most frequent request path, and write the JSON report."
verification_explanation = "The verifier independently derives the expected values from access.log and checks the report schema and each required result."

[verifier]
timeout_sec = 30.0

[environment]
allow_internet = false
mcp_servers = []
